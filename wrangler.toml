# 项目名称（自定义，建议和游戏名称一致）
name = "cfnewup"

# Workers 入口文件路径（确保和实际文件名一致）
main = "_worker.js"

# 兼容性日期（使用最新日期或稳定版本日期）
compatibility_date = "2025-12-18"

# 开发环境配置（本地调试用）
[dev]
# 本地运行端口
#port = 8787
# 自动重载代码
#autoreload = true
